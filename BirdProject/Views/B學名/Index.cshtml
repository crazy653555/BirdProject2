@model IEnumerable<BirdProject.Models.B學名>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Bird學名)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Bird學名)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Bird學名Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Bird學名Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Bird學名Id })
        </td>
    </tr>
}

</table>
